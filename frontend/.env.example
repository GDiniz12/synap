# ==============================================
# SYNAP FRONTEND - ENVIRONMENT VARIABLES EXAMPLE
# ==============================================

# URL privada do Backend no Render (lida apenas pelo servidor Next.js no Vercel via Rewrites)
# Exemplo no Vercel: https://synap-backend.onrender.com
# Exemplo em desenvolvimento local: http://localhost:3000
API_BACKEND_URL=http://localhost:3000

# (Opcional) URL pública direta caso não queira usar o Proxy Reverso
# NEXT_PUBLIC_API_URL=http://localhost:3000/api
