# Development placeholders for optional AI integrations.
# These only unblock startup; the AI features won't work without real keys.
# Override real values via the Base44 secrets dashboard.
SYNAP_AI_API_URL=http://localhost:8000/v1
SYNAP_AI_API_KEY=
OMNIROUTE_API_URL=http://localhost:8000/v1
OMNIROUTE_API_KEY=
